{
  "name": "review-analysis-tool-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "test:e2e": "npm --prefix frontend run test:e2e"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
